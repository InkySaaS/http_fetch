name: my_app
description: A new Flutter project.

publish_to: 'none' # Remove this linha se você quiser publicar para pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  # Adicione o pacote http aqui
  http: ^0.13.5

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
